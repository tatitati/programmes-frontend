<nav class="schedule-nav-wrapper fixedsticky">
    <div id="schedule-daynav" class="g-o-l centi br-box-highlight">
        <ol class="nav nav--banner date-list {{ date_list.option('css_classes') }}">
            {%- for offset in -7..7 -%}
                {%- set item = date_list.dateListItem(offset) -%}
                {%- include item.templatePath with { (item.templateVariableName): item } only %}
            {%- endfor -%}
        </ol>
    </div>
</nav>
